LEARN GIT IN 13 STEPS

Step 1
Make sure git is installed locally .
https://git-scm.com/downloads
Open a CMD /bash in any working folder and type " git --version "



Step 2 Changing the settings

git config --global user.email "email"
git config --global user.name "username/josekavunkal123"


Step 3
git init 

Step 4
Login to github  https://github.com/ and create new repository .Better name it "gitlearn" 
Select "Code " and copy link https://github.com/josekavunkal123/gitlearn.git

Step 5
git remote add origin  https://github.com/ZainabAlharbi/Final_Project.git


Step 6 //Add and commit
Just add a simple text file in gitlearn folder .
git status 
git add .  // recurssively add all files 
git commit -m " updated new files "
git push --set-upstream origin master  // set the remote as upstream and push 
 
Step 7 //Edit the file  //Default is master

Edit the file we have added 
git status 
git add .  // recurssively add all files 
git commit -m " updated new files "
git push origin


Step 8.  // get uploaded files 

Add a file to remote git via browser .
git pull origin


Step 9 . Delete and download the files again .

Delete all the downloaded files .
git fetch --all
git reset --hard


Step 10 //Learn difference between pull and fetch 
Edit a file in remote git using web browser or you can add a file .

git fetch 

//see if the local file has changed .No files will be changed .
Then

git pull 


Following two steps will create branches and switch between it .


Step 11.Create a branch and set push upstream

git checkout -b "mybranch1"
//upadte any locate files.
git add .
git commit -m "comment "

git push --set-upstream origin mybranch1
git branch


Step 12://Create a branch and set push upstream

git checkout -b "mybranch2"
//upadte any locate files.
git add .
git commit -m "comment "
git push --set-upstream origin mybranch2
git branch


Step 13 ://switch between branches
git checkout mybranch1
//upadte any locate files.
git add .
git commit -m "comment "
git push --set-upstream origin mybranch1
 


